---
name: backlog-show
description: Assemble the picture of one work item — its details, outcomes, tasks, journal and what would move it along — or of one board column, everything sitting at those statuses and whether the column is healthy. Use when asked about a work item by name or key, when picking work back up, when handing over, or when somebody asks "where is X", "what's left on X", "what's in progress", "what's up next", or "what's in To Do". Do NOT use to change anything.
---

<!-- luma-foreman:generated from local/backlog procedure/backlog-show. Regenerate with `luma-foreman apply`; edits are lost. -->

# Show where a work item stands

**Read `.luma/bundles/local/backlog/procedure/backlog-show.md` and follow it.** That file is the procedure. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

From the `local/backlog` bundle, vendored at `.luma/bundles/local/backlog/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
