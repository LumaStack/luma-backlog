---
name: record-a-violation
description: File a violation record in under a minute — the directory, the six fields, and the one question that decides what it means. Use when an agent did something that was not wanted, whether or not a written rule covers it.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/violation-records procedure/record-a-violation. Regenerate with `luma-foreman apply`; edits are lost. -->

# Record a violation

**Read `.luma/bundles/lumastack/luma-catalog/violation-records/procedure/record-a-violation.md` and follow it.** That file is the procedure. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

From the `lumastack/luma-catalog/violation-records` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/violation-records/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
