# Journal — A ledger of attempts against an outcome

> The work item's memory. Newest entry first; everything below the top block is
> historical. Append, never curate. Shape: `spec.md` §5.5.

---

## ▶ 2026-09-06

the list half shipped early — appending costs the same as replacing since verified already appends, so only the wave binding stays deferred
