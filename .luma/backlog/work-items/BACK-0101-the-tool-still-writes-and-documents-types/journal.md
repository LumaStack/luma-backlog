# Journal — the tool still writes and documents _types

> The work item's memory. Newest entry first; everything below the top block is
> historical. Append, never curate. Shape: `spec.md` §5.5.

---

## ▶ 2026-09-20

Found immediately after the estate migrated: init.go still scaffolds the retired layout, so **every newly initialized project starts on it**. That is the sharper half --- documentation drifting is a correction, but a scaffold writing a retired layout manufactures new drift with every init, and nobody notices until that project migrates.
