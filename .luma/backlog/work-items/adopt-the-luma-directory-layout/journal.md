# Journal — Adopt the luma directory layout

> The deliverable's memory. Newest entry first; everything below the top block is
> historical. Append, never curate. Shape: `SPEC.md` §5.5.

---

## ▶ 2026-09-02

migrated: bundle at .luma/backlog/ (config.yml and _types stay with the bundle, per the policy's own distinction from .luma/_types), top-level decisions at .luma/records/decisions/, write ceiling widened to .luma/ — cross-tier wikilinks are now a format question to feed back
