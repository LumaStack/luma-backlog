# Journal — Ask the backlog what is open

> The work item's memory. Newest entry first; everything below the top block is
> historical. Append, never curate. Shape: `spec.md` §5.5.

---

## ▶ 2026-09-08

built as --open, which mvp.md's command table had already chosen over a filter negation or a changed default — the third would have altered what a bare list returns, and output shapes are contract
open is DERIVED, not configured: the terminal status is the last value in the configured vocabulary, mirroring DefaultStatusFor taking the first. a project that renames closed keeps working, and one that adds a rung after it moves the terminal with no key to remember
a record with no workflow_status reads as the first rung, which is never terminal — so absence is open, and only an explicit terminal value is closed; there is a test for it
the numbers on this corpus: 59 open, 13 closed, 72 total — and WORK-0016's own problem statement said eleven of sixteen were closed, so the ratio it complained about has held while the corpus quadrupled
reopened to close honestly — it was closed with 'set workflow_status=closed', which never runs close's gate, so a work item with no outcomes ended as closed without the refusal that exists to prevent exactly that
outcomes written after the fact and proven, which WORK-0032 ranks second of three and permits — the renamed-terminal clause was checked against a scratch corpus whose ladder ends in 'finished' rather than asserted from the code

## ▶ 2026-09-05

raised after answering 'what is open' with a python pipe several times in one session — the tool could not express its own most common question
