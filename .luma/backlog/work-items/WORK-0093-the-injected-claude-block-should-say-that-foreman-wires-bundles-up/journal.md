# Journal — The injected CLAUDE block should say that foreman wires bundles up

> The work item's memory. Newest entry first; everything below the top block is
> historical. Append, never curate. Shape: `spec.md` §5.5.

---

## ▶ 2026-09-10

written from a failure an hour old: an agent added a rule to a bundle policy, judged it would not reach a plain turn, and hand-wrote a copy into CLAUDE.md outside the markers. reverted by the maintainer
nothing in the injected block would have prevented it — 'edits between the markers are lost' warns about WHERE to edit and reads as permission for everywhere else. what was missing is that a rule does not belong in that file at all
LIVE INSTANCE found while capturing: INDEX.md says local/backlog 0.27.0 and the bundle is at 0.36.0 — nine versions, all written today, in the one file always in context. apply has not run, and nothing reports it
that the index carries versions at all is what made it checkable. an index listing bundles without versions would have been silently wrong instead of visibly so
this record is the instance behind WORK-0094 — it was asked for in luma-foreman and created in luma-backlog
