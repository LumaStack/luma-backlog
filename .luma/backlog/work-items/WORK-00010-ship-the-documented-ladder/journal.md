# Journal — Ship the documented ladder

> The work item's memory. Newest entry first; everything below the top block is
> historical. Append, never curate. Shape: `spec.md` §5.5.

---

## ▶ 2026-09-04

implementing ADR-0002 as-is; the three names it left open are settled by benjamin on 2026-09-04 — captured, unprepared, prepared
corpus migrated deliberately rather than mechanically: three records went to unprepared because they were decided in conversation, and lint-the-corpus to captured because it was fanned out and what is left is the capture
the default is captured, which is honest in a way idea was not — new writes down a record so it is not lost, and that is exactly what the rung says happened
