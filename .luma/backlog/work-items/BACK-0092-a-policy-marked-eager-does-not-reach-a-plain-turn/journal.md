# Journal — A policy marked eager does not reach a plain turn

> The work item's memory. Newest entry first; everything below the top block is
> historical. Append, never curate. Shape: `spec.md` §5.5.

---

## ▶ 2026-09-10

found 2026-09-10 while adding a rule about citing record keys to showing-records. the agent reached for CLAUDE.md to make it fire and the maintainer stopped it — a hand-written rule there is unversioned, drifts from the bundle it copies, and bypasses the apply step that is supposed to carry a rule to where it is read
which left the real question exposed rather than solved: the rule is now in the right place and may not reach the case it exists for. today it would have caught none of the bare keys, because they were all in plain replies
if the second reading is right, the fix is not a rule anywhere new — it is that citing a record is itself a trigger to open the bundle governing records, which is a triggering question rather than a delivery one
WORK-0093 is the other half of this: telling an agent not to edit CLAUDE.md closes the wrong door if the reason it reached for CLAUDE.md — a policy that never loads — is left open. one question, two halves: what not to do, and what to do instead
