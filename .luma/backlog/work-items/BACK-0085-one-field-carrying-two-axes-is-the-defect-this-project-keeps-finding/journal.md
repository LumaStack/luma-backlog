# Journal — One field carrying two axes is the defect this project keeps finding

> The work item's memory. Newest entry first; everything below the top block is
> historical. Append, never curate. Shape: `spec.md` §5.5.

---

## ▶ 2026-09-10

FIFTH instance, and this one is the inverse rule rather than the two-axes one: on WORK-0081 the agent passed --reason on a completed close AND wrote a closing wrap saying the same thing. two copies of one fact, which ADR-0003 already names
the fix was to decide where the fact LIVES rather than to forbid the duplicate: the journal holds reasoning, the close reason holds what the enum cannot, and a completed close has the outcomes so it needs neither. backlog-move 0.34.0 says so
