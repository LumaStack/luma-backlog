# Journal — Two attempts at the same task cannot both exist

> The work item's memory. Newest entry first; everything below the top block is
> historical. Append, never curate. Shape: `spec.md` §5.5.

---

## ▶ 2026-09-06

parked deliberately — the system does not run yet, and a single maintainer means whatever is chosen later costs one rename pass; the resolution may be to narrow section 9.5 rather than add identity
