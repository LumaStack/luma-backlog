# Journal — Adopt the luma directory layout

> The work item's memory. Newest entry first; everything below the top block is
> historical. Append, never curate. Shape: `SPEC.md` §5.5.

---

## ▶ 2026-09-04

closing: the four tiers exist, .backlog is gone from disk and from the code, and init scaffolds the layout in a fresh repository
one claim in this record went stale rather than unmet: the types were delivered to bundles/luma-backlog/_types/ and later moved to bundles/local/backlog/, which is a subsequent change and not a gap in this work
the outcome here was declared at close rather than before the work, so read it as a description of what was done and not as the bar the work was held to

## ▶ 2026-09-02

migrated: bundle at .luma/backlog/ (config.yml and _types stay with the bundle, per the policy's own distinction from .luma/_types), top-level decisions at .luma/records/decisions/, write ceiling widened to .luma/ — cross-tier wikilinks are now a format question to feed back
