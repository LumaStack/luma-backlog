# Journal — Work the backlog from a terminal

> The work item's memory. Newest entry first; everything below the top block is
> historical. Append, never curate. Shape: `spec.md` §5.5.

---

## ▶ 2026-09-07

a pipelines view was proposed as its own command and is absorbed here — workflow-status.md names two pipelines and .luma/config's columns already map one-to-one onto their rungs, so --column plus grouping is that view; re-open it as separate only if the two pipelines need something columns cannot express, such as the gates between them
