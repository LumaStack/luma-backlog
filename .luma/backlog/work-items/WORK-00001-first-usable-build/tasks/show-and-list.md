---
type: task
title: show and list
work_item: "[[work-items/WORK-00001-first-usable-build]]"
advances: ["[[work-items/WORK-00001-first-usable-build/outcomes/the-backlog-is-kept-by-the-tool]]"]
rank: "0070.000"
parallel_group: [commands]
workflow_status: closed
---

# `show` and `list`

Read one, read many. Human-readable by default, `--json` for callers.

**The `--json` shapes are the contract**, so each gets a golden file as it is written rather than afterwards — a shape pinned later is pinned to whatever it happened to become.
